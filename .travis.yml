language: cpp
compiler:
    - clang
    - gcc
before_install:
    - sudo apt-get update -qq
    - sudo apt-get install -y libboost-all-dev
before_script:
    - mkdir build
    - cd build
    - cmake ..
script: make
after_script:
    - ctest
